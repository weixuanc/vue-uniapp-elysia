# =====================================================
# 环境变量示例
# =====================================================
# 此目录包含各环境配置示例。
#
# 使用方式：
#   开发环境：cp .env.development .env.development.local 并按需修改
#   生产环境：cp .env.production    .env.production.local    并按需修改
#
# .env.local 为本地私有配置，不会被 Git 跟踪。
#
# 可用变量：
#   VITE_VERSION             应用版本号（构建注入 __APP_VERSION__）
#   VITE_PORT                开发/预览服务器端口
#   VITE_BASE_URL            应用部署的基础路径
#   VITE_API_URL             axios 请求 baseURL
#   VITE_API_PROXY_URL       开发服务器代理目标
#   VITE_WITH_CREDENTIALS    axios 是否携带 cookie
#   VITE_ACCESS_MODE         访问模式：frontend | backend
#   VITE_USE_MOCK            是否启用 Mock
#   VITE_USE_GZIP            是否生成 .gz 压缩文件
#   VITE_USE_CDN             是否启用 CDN 加载
#   VITE_LOCK_ENCRYPT_KEY    锁屏加密密钥
#   VITE_OPEN_ROUTE_INFO     是否在控制台打印路由信息
